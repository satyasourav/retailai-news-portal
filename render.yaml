services:
  - type: web
    name: retailai-news-portal
    runtime: node
    buildCommand: |
      cd frontend && npm install && npm run build
      cp -r frontend/dist backend/public
      cd backend && npm install
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
